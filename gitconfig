[user]
    name = Joseph Muia
    email = joseph.b.muia@gmail.com

[alias]
    assume   = update-index --assume-unchanged
    unassume = update-index --no-assume-unchanged
    assumed  = "!git ls-files -v | grep ^h | cut -c 3-"

[push]
    default = simple
